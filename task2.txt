CODEKATA PRACTICE..

1.To take an integer value and Print the integer value.

    	let number = userInput[0];
    	console.log(number);

or

	let number = 2;
	console.log(number);

2.A single line contains integers separated by space and Print the integer list of integers separated by space.


  	let number = userInput[0].split(userInput);

  	console.log(userInput[0]);

3.First-line indicates two integers which are the size of array and 'K' value. Second-line indicates an integer contains elements of an array.

	let number = userInput[0];
  	let number1 = userInput[1];
    	console.log(number);
    	console.log(number1); 

4.First-line indicates two integers separated by space. Second-line indicates two integers separated by space. Third-line indicates two integers separated by space.

	 let x = userInput[0];
 	 let y = userInput[1];
  	let z = userInput[2];

  	console.log(x);
  	console.log(y);
  	console.log(z);
